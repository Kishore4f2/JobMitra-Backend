# Server Configuration (For Render/Railway)
server.port=${PORT:8080}

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:mysql://mainline.proxy.rlwy.net:47808/job_portal?allowMultiQueries=true&createDatabaseIfNotExist=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:RuofrQBcgYMZSkHKIqOEKsYcipBIUXpA}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Schema Initialization
# Ensures the database matches the code structure on startup
spring.sql.init.mode=never
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.continue-on-error=true

# Debugging
spring.jpa.show-sql=true
logging.level.org.springframework.jdbc.core=DEBUG
spring.jpa.hibernate.ddl-auto=none

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Frontend URL for CORS & OAuth Redirects (Supports Dev/Prod environments)
app.frontend.url=${FRONTEND_URL:https://jobmitra-find-your-way.vercel.app}

# OAuth2 Configuration (Google & GitHub)
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:304780521127-bpf1behddjdcin53bq6cgmh4ktoj40f7.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email

spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID:Ov23liq2BIIlZIxkewrJ}
spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_SECRET}
spring.security.oauth2.client.registration.github.scope=read:user,user:email

# ─── SMTP Mail Configuration (Gmail) ──────────────────────────────────────────
# Use Google App Password (16 chars, spaces removed) — NOT your real Gmail password.
# Set MAIL_USERNAME and MAIL_PASSWORD as environment variables in Render/Railway.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:satyakishore273@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nvbrggfgrfkiihsj}
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
spring.mail.default-encoding=UTF-8

